import { LANDING_PAGE_MENU, MenuProps } from "./menu"

type SkillWiseConstantsProps = {
    landingPageMenu: MenuProps[]
}

export const GROUPLE_CONSTANTS: SkillWiseConstantsProps = {
    landingPageMenu: LANDING_PAGE_MENU,
}
